/* This js file allows a transaction's details to be viewed in the transactions index page. */
$("#ajax-desc").html("<%= escape_javascript(render :partial => 'show', :locals => {:transaction => @transaction}) %>");
